# 更新日志

本文件记录 MyDataDev 每个发行版本的变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

0.5.0 之前的条目是从 git 历史回溯整理的（此前没有维护本文件），因此越早的版本描述越粗；从 0.5.0 起，发版时应同步更新这里。

## [0.7.0] - 2026-09-08

本版本没有新增大功能，改的都是用起来会硌手的地方：SQL 编辑器的补全从「把词全倒出来」变成按光标位置给、结果集就地编辑在 Oracle 上第一次真正能用、以及一处会让 SSH 隧道连错主机的隔离缺口。

### 新增

- **Oracle 上的查询结果也能就地编辑了**。ojdbc 对每一列的 `getTableName()` 都返回空串，所以这个功能在 Oracle 上从来没能用过。现在驱动一列都不报表名时退回按 SQL 文本认来源表：只接受最没有歧义的形状（单条 SELECT、FROM 后正好一张物理表，没有 JOIN／子查询／表函数／dblink／WITH／集合运算／DISTINCT／GROUP BY），并把未加引号的标识符按方言折成库里实际存的形态（Oracle 折大写、PostgreSQL 折小写）。认出表名之后仍要过三道校验才发编辑令牌：投影是直投影、每一列都是这张表真实存在的字段、主键字段在结果集里 —— 任何一道不过就不给编辑。
- **补全候选带上注释、来源与类型**：表和字段候选显示注释摘要与来源，完整备注可展开；SQL 结果区和表数据页的列头也给出备注与类型提示。新增字段元数据接口与前后端共享缓存，并发加载会合并，切连接、切 Schema、切标签页的请求互相隔离。Oracle 的表与字段注释一并补上。

### 变更

- **关键字候选改为按光标位置给**。之前不管光标在哪里都把同一份 17 个词整份倒出来 —— 一条语句写完、光标停在分号后面，弹出来的是 AND / FROM / GROUP BY / HAVING / INNER JOIN，那个位置一个都接不上。现在只看两件事：光标前那个词和往回找到的最近一个子句关键字。该写表达式或表名的位置一个关键字都不给（那里有用的是字段和表候选），认不出来的位置退回整份清单 —— 宁可多给，也不要把真正能用的词筛掉。
- **没有正在输入的词时不再自动弹出候选**。刚敲完 `;` `(` `,` 并不是在写词，此时摊开一列候选纯属打扰。三处有意为之的触发照旧：Ctrl/Cmd+Space、`别名.` 之后、条件关键字后的空格。
- **说清楚结果为什么不能就地编辑**：驱动一列都不报表名与各列报的表名对不上，是两件完全不同的事，之前一律说成「查询结果不是来自单张表」—— 在一条单表查询上给出与事实相反的说明，用户只会去 SQL 里找那个并不存在的联表。

### 修复

- **SSH 客户端不再继承运行账户的 `~/.ssh`**。数据库 SSH 跳板机那条路用的是裸的 `SshClient.setUpDefaultClient()`，它的 host 配置解析器直接指向 `~/.ssh/config`，启动时还会自动补上 `~/.ssh/id_*` 的私钥监视器。于是同一份连接配置换台部署机就可能连向别的主机（HostName／Port／User／ProxyJump 都会被改写），或者拿服务器账户自己的私钥通过认证 —— 后一种更糟：配置里的凭据早就作废了，隧道却还连得上，而隧道后面是业务库。现在全后端只有 `SshClients.newIsolatedClient()` 一个入口，并有源码扫描测试守着。
- **只读连接下 SQL 编辑器塌成一行**：`.sql-workspace` 的只读变体还按五行排版，而那条只读提示 Alert 早就删了，于是整体错位一格 —— 编辑器被裁到只剩 36px，状态栏浮在中间。
- **不再把「客户端断开」记成服务器错误**：Web 发行包空跑时控制台定期刷出的整页错误栈，来源是后台任务 SSE 往一个已经走了的浏览器写。刷新页面、关标签页、反向代理掐掉空闲长连接都会走到这条路上 —— 这不是故障，可它记成 ERROR，真正的故障反而淹在里面。判定不看消息文本（那由操作系统给，中文 Windows 上是另一句话），只看异常类型和栈里有没有往客户端 socket 写的帧。
- **FTP 与 SFTP 备份改用登录账户根目录下的相对路径**，不再写入受限服务器根目录；`printWorkingDirectory()` 返回 null 时不再发一条没有参数的 CWD，那会让服务端回 501、用户看到一句指错方向的报错。
- **SQL 字段联想的候选缓存回归**：按前缀筛过的候选被当成完整列表沿用，删除或修改前缀时会丢字段；元数据加载失败后继续输入也无法重试。
- 桌面冒烟增加超时、退出码与可观测输出，看门狗定时器改在后端真正关掉之后再解除 —— 否则 CI 机器上会留下一个没人回收的后端进程。

### 升级注意

- 本版本没有新增元数据库迁移（schema 仍是 v20），从 0.6.0 升级直接替换发行包即可。

## [0.6.0] - 2026-09-06

本版本有两条主线：把 AI 从「一问一答」做成能自己查结构的 Agent，以及补上几件日常排查里一直得切到别的工具才能做的事 —— 数据搬到另一条连接、在全库里找一个值、看清是谁堵了谁、这个字段是什么时候加的。

### 新增

- **AI 助手**：工作台内置模型能力。自然语言转 SQL 是一个服务端闭环的 Agent —— 按需搜索表/字段注释、读取字段与外键、参考这个库的历史查询口径（进模型前先抹掉字面量），生成前做一次编译校验；需求有歧义时经 `ask_user` 反问而不是猜。执行报错诊断、执行计划解读（唯一允许产出一条 `CREATE INDEX` 的场合）与结果复盘都并入同一段会话，另有结果解读与图表推荐、数据字典生成、同步脚本风险说明。**AI 没有执行权限**，产出只落到新编辑器标签页。
- **AI 的数据边界与用量**：连接默认「不参与 AI」，由管理员逐条授权；「仅结构」档不发任何行数据，样本档受行数上限约束且生产连接禁用。服务商可选官方 Claude API 或 OpenAI 兼容协议。业务词典的候选从表注释推出来供审阅，「搜了却什么都没搜到」的词单独记下来；token 用量与日额度闸门在管理抽屉里。
- **跨连接数据传输**：把 A 库的一张表或一段查询结果搬到 B 库的表里，不经 CSV 中转（空串与 NULL 因此分得清）。读侧沿用导出的规矩，写侧沿用后台 SQL 任务，进度、取消、并发闸门、生产确认与两端审计都不另起一套。
- **全库数据检索**：在一个 Schema 的所有表里找一个值。比较语义与结果表格的筛选同源，每张命中表回传一条可直接执行的查询；表数、每表取样行数与总时间预算三条上限任何一条触顶都会报告「扫到一半停了」。
- **结构快照与漂移追踪**：按 cron 或手动给一个 Schema 存结构快照，回答「这个字段是什么时候加的」。结构没变时只推进「最后一次见到」的时间、不新增记录；漂移判定与结构对比同一套逻辑。
- **数据对比与同步**：两张表逐行比对（按主键哈希匹配，不做归并），列出只在一侧与字段不同的行，按目标端方言生成同步脚本，默认不生成 DELETE。
- **锁与阻塞分析**：活动会话之外再给出「谁在等谁」——把直接阻塞关系连成链，每个节点标出下游被堵的会话总数，互相等待的环单独报出来。阻塞关系读不到不影响会话列表。
- **存储过程与函数的编译错误与输出**：Oracle 上语法错误也会「创建成功」，所以创建后回查数据字典报出编译错误、编辑器不关闭；例程自己打印的输出（PostgreSQL `RAISE NOTICE`、MySQL 警告、Oracle `DBMS_OUTPUT`）一并显示。
- **定时导出**：按 cron 或手动提交后台查询导出，支持取消、运行历史与文件下载。只接受单条查询，生产连接的确认在创建任务时完成。
- **Excel 导入**：`.xlsx` 与 CSV 共用同一条后台导入管线（含日期样式解析）。导入新增两种主键冲突策略：跳过重复行、更新已存在的行；默认仍是直接插入。
- **命令面板**（`Ctrl/Cmd+K`）：搜索应用能做的事 —— 管理抽屉的每个分区、SQL 工具条上的动作、切换连接；不可用的命令也会列出并说明原因。
- **备份历史「校验」**：把备份文件完整读一遍，比对大小与 SHA-256。这是文件级校验，不是恢复演练。
- **连接池可见性**：名额被哪条连接占着、哪个池早就闲了，摆在连接管理里，而不是等到报出 `REMOTE_POOL_EXHAUSTED` 才去别处找。
- **SQL 历史统计**：最慢、失败最多、谁在跑三个视角。
- **表工作区导出当前筛选后的数据**：服务端把浏览用的 SQL 去掉分页重跑，导出内容与屏幕上的筛选、排序完全一致。
- **列注释可在表设计器里修改**（SQLite 与 SQL Server 不支持，界面不显示这一列）。
- **编辑器给未知表名画波浪线**：只在对象清单确实完整时提示，宁可不提示也不误报。
- **桌面端启动时检查新版本**。
- **界面冒烟脚本**（`scripts/ui-smoke.mjs`）：用系统 Chrome 拉起 Web 发行包，从选连接一直走到执行查询与导出，不引入 Playwright；CI 一并接上。

### 变更

- **主密钥由系统托管**：Web 与后端开发模式首次启动自动生成权限受限的 `./secrets/mydatadev-master.key`，后续自动复用；部署平台也可通过 `app.crypto-key-file` 挂载只读 Secret。桌面版继续使用操作系统安全存储，但改由标准输入把密钥一次性交付给内置后端，不再写入子进程环境变量。启动时会验证元数据库中的全部历史密文，缺少或使用错误密钥时安全拒绝启动，不会自动覆盖密钥或修改历史数据。
- **SQL 结果的排序与列筛选下推到服务端**，与表浏览那条路语义一致：改排序或改筛选时 offset 归零，结论作用于整个结果集而不是当前这一页。结果里有同名列或不支持翻页时仍走本地，界面上标出来。
- **单轮的「自然语言转 SQL」与「执行计划解读」并入 Agent**，两条单轮路径删除 —— 只看一条 SQL 或一段计划文本，模型看不到这张表上真实存在哪些索引。
- 单元格文本化规则合一到 `core/CellSerializer`，修掉执行计划与导出之间的时间格式漂移。
- 查询、分页、脚本、执行计划四条路加上 Micrometer 指标，超时单独成一类。
- **管理抽屉导航按四组归类**（连接与运维 / 数据与结构 / 智能与集成 / 安全与治理），命令面板复用同一份分类；分区已经从 5 个长到 14 个，平铺一列扫不动了。
- **审计日志改为一行一条**：同样的一屏从 10 条变成 19 条。
- **表格列宽不再被均摊拉伸**：列宽之和填不满视口时，多余宽度交给尾部一列空白，两位数的 ID 列不会再被拉到 300px；结果表的列宽也把表头上的排序与筛选按钮算了进去，`CUSTOMER` 不会再显示成 `CUSTOM…`。

### 修复

- 资源树切换页签后列表变短。
- 两处会给出错误结论却不报错的故障，并补上漏收的第五条单元格路径。
- SQL 片段库点击无响应；对象搜索在一次重构后失效。
- 查询结果区分隔条拖动性能。
- SQL 工作台标题与副标题因样式层叠贴成一行。
- **审计链在部分 Linux 机器上会把每一条记录都报成被篡改**：`audit_log.created_at` 是精度到微秒的 `TIMESTAMP`，而 Linux 上 `Instant.now()` 可能给到纳秒，入库时被 H2 四舍五入（实测 …123456789 存成 …123457000）；哈希算的是截断前的值，校验时用读回来的值重算，于是从第一条起就对不上。这个差异只在时钟精度高于列精度的机器上出现，所以在 macOS 上不复现。现在哈希前先降到微秒 —— 哈希什么就存什么。
- **发布流水线的 Web 烟测仍在设置已停止支持的 `DB_ADMIN_CRYPTO_KEY`**，带着它启动会被直接拒绝 —— 这一条会让整个 Release 作业发不出来。

### 升级注意

- `DB_ADMIN_CRYPTO_KEY` 与 `app.crypto-key` 不再受支持。旧安装首次升级前必须运行 `java -jar MyDataDev-<version>-web.jar crypto-key adopt`，输入原密钥；候选密钥只有在下次启动验证全部历史密文成功后才会成为正式主密钥。

## [0.5.0] - 2026-08-30

本版本的重心是 Web 多用户安全模型与一轮系统性的安全走查。

### 新增

- **Web 多用户认证与授权**：内置账号体系、管理员/操作员角色、服务端会话与 CSRF 防护；支持通用 OIDC 单点登录（用户名、显示名、角色与用户组由身份提供器同步）。
- **连接级权限**：按用户或用户组授予元数据查看、查询、数据写入、DDL、导出、备份恢复与连接管理七类权限。
- **审计哈希链**：审计记录串联 SHA-256，可检测删除、插入、字段修改与顺序篡改；审计带请求来源、User-Agent 与请求 ID。
- **MCP 分级授权**：Agent 的连接授权从「白名单」升级为「白名单 + 访问档位」（只读 / 数据读写 / 完全），档位按连接授予，同一个 Agent 可以在开发库上有写权限、在生产库上只读。新增写操作工具 `db_execute`，复用界面同一条执行路径。
- **Schema ER 图**：按外键自动分层绘制实体关系图，不引图表库（自绘 SVG）。
- **连接配置加密导出与导入**：把连接（含 SSH 隧道）打成一个用口令加密的归档文件，在另一台装机上导入。归档用口令而非本机主密钥加密——目标端的密钥必然不同。仅管理员可用，两端都写审计。
- **Markdown 表格导出**：查询结果可直接导出为可读的 Markdown 表格，数值列右对齐。

### 变更

- **SQL 编辑器从 Monaco 迁移到 CodeMirror 6**：同等能力下编辑器资源块 gzip 从 685 KiB 降到 125 KiB。补全、语法高亮、格式化、行注释、定义跳转与快捷键行为保持不变。
- 编辑器接口改为项目自有的窄抽象（按字符偏移量表达），业务代码不再直接依赖某个编辑器实现。
- 表数据查询改为服务端筛选与排序。
- `/api/access/me` 从每条连接 8 次查询改为一次查询后在内存推导。

### 修复

一轮代码走查修复了 13 处安全与稳定性缺陷，其中三处是可直接利用的越权：

- 手动事务可被他人劫持：`/api/sql/transactions/active` 泄露事务 id，且执行/提交/回滚只校验连接权限、不校验归属。
- 登录锁定被 `X-Forwarded-For` 轮换绕过，且失败计数表无上限。
- SQL 文件任务在分析期（计数为 0）完全不校验权限。

其余包括：管理员编辑用户组会切断 OIDC 撤销链路、OIDC 模式可能一个管理员都没有、CORS 通配来源配合会话 Cookie、审计链遇到空哈希会永久停写、`verifyChain()` 全表载入并阻塞审计写入、V11 迁移的 JDBC batch 无上限、SSO 账号编辑静默失败、审计导出被截断时无提示。

此外还修复了：不同用户的恢复上传文件未隔离、连接全量 SQL 历史可被越权访问、Web 认证模式下 SQL 导出失败、SQL 恢复失败时的数据一致性、已取消的备份被迟到状态覆盖、并发操作可移除全部管理员、审计告警的内存与发送放大、前端构建链依赖漏洞、结果表格列宽拖动清理、CodeMirror 迁移后文本选区不可见。

### 升级注意

两处配置校验会让不合规的部署**直接启动失败**，均为有意设计：

- 启用 OIDC 时必须配置 `app.auth.oidc.admin-groups`。角色每次登录都按该配置重算并写回账号，组为空意味着所有 SSO 用户都是 `OPERATOR`，管理、审计与 MCP 页面从此无人可进，而 SSO 账号的角色不接受手工修改。
- 启用 Web 认证时 `app.cors.allowed-origin-patterns` 不能匹配任意来源（`*`、`https://*` 等）。跨域请求现在会带会话 Cookie，而 `/api/auth/status` 会返回 CSRF 令牌。子域通配（`https://*.corp.example`）不受限制。

**既有 MCP Agent 的能力不变**：迁移把所有已有连接授权落到只读，写权限必须在界面上显式授予。

## [0.4.0] - 2026-08-28

本版本的重心是把工作台从「能查」补齐到「能改、能对比、能追溯」。

### 新增

- **查询结果就地编辑**：分页浏览时直接改行，提交前预览 SQL，对无稳定行标识与危险更新有保护。
- **手动事务**：把一个标签页里的多次执行绑到同一条连接与事务上，先看效果再决定提交或回滚。
- **SSH 隧道**：目标库只对跳板机开放时由后端建立本地端口转发，支持口令与私钥认证及主机指纹校验。
- **结构对比与同步**：比较两个连接/Schema 的表、字段、索引与主键差异，生成迁移 DDL 交由 SQL 工作台执行。
- **查询结果图表**：柱状图、折线图、饼图，色板经色觉障碍校验。
- **Excel（.xlsx）导出**、**保存的 SQL 片段库**、**跨对象类型的全局搜索**（Ctrl/Cmd+P）、**目标库活动会话视图与终止会话**、**审计日志读取入口**、**后台任务指示器**。
- **PostgreSQL 原生备份与恢复**（pg_dump / pg_restore）、**大文件 CSV 导入**接入后台执行框架。
- 连接配置增强：分组、标签、默认命名空间、会话初始化 SQL、备注。
- 外键从「只能看」变成「能走」；执行计划从原样铺表变成可读的解读。

### 变更

- 管理类面板合并成一个带左侧导航的抽屉。
- UI 一致性整顿：设计令牌、统一空状态、抽屉三档宽度、主色收敛、表格密度与数值对齐。
- 审计记录的连接归属改为独立字段，修复按连接筛选漏记录。
- 审计写入移出请求线程；表数据分页支持按需统计总行数。

### 修复

多轮走查累计修复约 20 处缺陷，其中影响较大的有：生产连接因中文命名完全不可用、可写 CTE 绕过「无 WHERE 条件」二次确认、EXPLAIN 把普通查询误判为写操作、池化连接的 catalog/schema 泄漏给下一个借用者、定时备份触发失败静默跳过执行窗口、后台任务取消竞态与备份重复提交。

## [0.3.0] - 2026-08-21

### 新增

- **Web 发行包**：单进程可执行 JAR，同源提供 UI、`/api` 与 `/mcp`，随桌面安装包一起发布。
- SQL 工作台支持对象定义跳转。
- 备份支持文件服务（SMB / NFS / FTP / SFTP）并完善远端恢复流程。

### 变更

- 重写 SQL 格式化排版；优化 SQL 补全分词与元数据缓存失效范围。
- 统一错误提示中文化，改善翻页、导出与 CORS 配置体验。

### 修复

定时备份按服务端时区触发、HTTP 环境下 SQL 执行标识生成失败、生产 SQL 确认弹窗无法继续执行、部署环境的连接测试与跨域错误、Oracle 备份元数据读取、旧版 SFTP 上传兼容。

## [0.2.0] - 2026-08-14

围绕 SQL 工作台与查询结果的交互和性能：结果多选与全选复制、行选中反馈、编辑能力、元数据访问与首屏性能、列表切换滚动状态，并修复了编辑器与结果表格的复制快捷键冲突。

## [0.1.1] - 2026-08-11

完善 macOS 无证书发布流程；补充 SQL 安全确认与结果导出复制能力。

## [0.1.0] - 2026-08-11

首个发行版本。多数据库连接管理、SQL 工作台、数据库对象浏览与详情、查询结果导出、备份与恢复、只读 MCP Server、Web 与桌面双模式。

[0.7.0]: https://github.com/tanner-sh/MyDataDev/releases/tag/v0.7.0
[0.6.0]: https://github.com/tanner-sh/MyDataDev/releases/tag/v0.6.0
[0.5.0]: https://github.com/tanner-sh/MyDataDev/releases/tag/v0.5.0
[0.4.0]: https://github.com/tanner-sh/MyDataDev/releases/tag/v0.4.0
[0.3.0]: https://github.com/tanner-sh/MyDataDev/releases/tag/v0.3.0
[0.2.0]: https://github.com/tanner-sh/MyDataDev/releases/tag/v0.2.0
[0.1.1]: https://github.com/tanner-sh/MyDataDev/releases/tag/v0.1.1
[0.1.0]: https://github.com/tanner-sh/MyDataDev/releases/tag/v0.1.0
